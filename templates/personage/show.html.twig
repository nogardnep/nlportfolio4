{% extends 'base.html.twig' %}

{% block title %}Personage{% endblock %}

{% block body %}
    <h1>Personage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ personage.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ personage.title }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('personage_index') }}">back to list</a>

    <a href="{{ path('personage_edit', {'id': personage.id}) }}">edit</a>

    {{ include('personage/_delete_form.html.twig') }}
{% endblock %}
